## 0.0.1

* Initial release of `flutter_ci_guard`.
* Quality gates orchestration: format, analyze, and test with coverage.
* Support for custom coverage thresholds via `--min-coverage`.
* CI/CD friendly exit codes.
* Zero external dependencies (only `args` and `path`).
